(function(){
'use strict';

const VERSION='0.9.4.60';
const DEFINITIONS_KEY='lotkeysAwardDefinitionsV1';
const NOMINATIONS_KEY='lotkeysAwardNominationsV1';
const PROFILE_KEY='lotkeysAwardProfileV1';
const PROGRESS_KEY='lotkeysProgressV1';
const CHECKIN_KEY='lotkeysCheckInV1';
const CREATOR_BINDING_KEY='lotkeysCreatorBindingV1';
const DEVELOPER_ACCESS_KEY='lotkeysDeveloperAccessV1';
const SPECIAL_AUDIT_KEY='lotkeysSpecialAwardAuditV1';
const CELEBRATION_EVENTS_KEY='lotkeysDeveloperCelebrationEventsV1';
const SEEN_CELEBRATIONS_KEY='lotkeysSeenCelebrationEventsV1';
const CATALOG=[
  {id:'new-kid-on-the-lot',name:'New Kid on the Lot',image:'./assets/awards/new-kid-on-the-lot.png',meaning:'Everybody’s gotta start somewhere — welcome to LotKeys.',criteria:'Hidden automatic welcome milestone.',method:'Automatic',repeatable:false,hiddenCriteria:true},
  {id:'no-newbie',name:'Tie Cut!',image:'./assets/awards/no-newbie.png',meaning:'You’re not a newbie anymore.',criteria:'Administration confirms your first vehicle sold while using LotKeys.',method:'Admin awarded',repeatable:false},
  {id:'cherrys',name:'Cherry’s',image:'./assets/awards/cherrys.png',meaning:'Two LotKeys-assisted vehicle sales landed on the same day.',criteria:'Administration confirms two vehicle sales in one day.',method:'Administration approved',repeatable:true},
  {id:'maaaaybeee',name:'Maaaaybeee??',image:'./assets/awards/maaaaybeee.png',meaning:'Sometimes the answer is… maaaaybeee??',criteria:'A one-off discretionary LotKeys moment recognized by Admin Level 2.',method:'Admin Level 2 awarded',repeatable:false,special:true},
  {id:'the-dark-knight',name:'The Dark Knight',image:'./assets/awards/the-dark-knight.png',meaning:'A one-off LotKeys recognition for someone who showed up when it mattered.',criteria:'A one-off discretionary LotKeys moment recognized by Admin Level 2.',method:'Admin Level 2 awarded',repeatable:false,special:true},
  {id:'hat-trick',name:'The Hat Trick',image:'./assets/awards/hat-trick.png',meaning:'Three signed deals in one day — hats off to a memorable day.',criteria:'Sign three deals in one day.',method:'Admin awarded',repeatable:true},
  {id:'almost-hat-trick',name:'“Almost” Hat Trick',image:'./assets/awards/almost-hat-trick.png',meaning:'For the day the expected hat trick took an unforgettable turn.',criteria:'Administration confirms an unexpected almost-Hat-Trick result.',method:'Admin awarded',repeatable:true},
  {id:'anti-celibratory',name:'Anti-Celibration',image:'./assets/awards/anti-celibratory.png',meaning:'Sorry man, you’ll sell the next one.',criteria:'Delete your first website-connected Vehicle Listing without marking it sold.',method:'Automatic',repeatable:false},
  {id:'dude-wheres-my-car',name:'Dude Where’s My Car',image:'./assets/awards/dude-wheres-my-car.png',meaning:'I Do-Be remembering that sharing is caring man.',criteria:'Create a Listing between 4:19 and 4:21, AM or PM.',method:'Automatic · hidden',repeatable:false},
  {id:'detail-detective',name:'Detail Detective',image:'./assets/awards/detail-detective.png',meaning:'Recognized for helping keep LotKeys Vehicle Profiles accurate.',criteria:'Reach 10 approved Vehicle Profile correction requests.',method:'Automatic',repeatable:false},
  {id:'folder-freak',name:'Folder Freak',image:'./assets/awards/folder-freak.png',meaning:'A major milestone in approved Vehicle Profile corrections.',criteria:'Reach 100 approved Vehicle Profile correction requests.',method:'Automatic',repeatable:false},
  {id:'faster-as-f-boy',name:'Faster as F Boy!',image:'./assets/awards/faster-as-f-boy.png',meaning:'You were faster than the source website’s price.',criteria:'After a successful source-page check explicitly reports “Contact the store” or another non-price placeholder, create a Facebook Marketplace Listing with a numeric price.',method:'Automatic',repeatable:false},
  {id:'woop-woop',name:'Woop Woop!',image:'./assets/awards/woop-woop.png',meaning:'Your first LotKeys-tracked Facebook Marketplace sale is in the books.',criteria:'Press “I Sold This Vehicle” on 1 Listing with its own valid, unique Facebook Marketplace URL. Non-Facebook and reused links do not count.',method:'Automatic',repeatable:false},
  {id:'mr-sales-man',name:'Mr. Sales Man',image:'./assets/awards/mr-sales-man.png',meaning:'Ten LotKeys-tracked Facebook Marketplace sales — nicely done.',criteria:'Press “I Sold This Vehicle” on 10 Listings, each with its own valid, unique Facebook Marketplace URL. Non-Facebook and reused links do not count.',method:'Automatic',repeatable:false},
  {id:'sales-50',name:'50 Looking 25',image:'./assets/awards/sales-50.png',meaning:'You sold your 50th car through a tracked Facebook Marketplace Listing.',criteria:'Press “I Sold This Vehicle” on 50 Listings, each with its own valid, unique Facebook Marketplace URL. Non-Facebook and reused links do not count.',method:'Automatic',repeatable:false},
  {id:'sales-100',name:'Happy 100th',image:'./assets/awards/sales-100.png',meaning:'You sold your 100th car through tracked Facebook Marketplace Listings.',criteria:'Press “I Sold This Vehicle” on 100 Listings, each with its own valid, unique Facebook Marketplace URL. Non-Facebook and reused links do not count.',method:'Automatic',repeatable:false},
  {id:'quarter-k-club',name:'Quarter K Club',image:'./assets/awards/quarter-k-club.png',meaning:'250 tracked Facebook Marketplace sales — welcome to the Quarter K Club.',criteria:'Press “I Sold This Vehicle” on 250 Listings, each with its own valid, unique Facebook Marketplace URL. Non-Facebook and reused links do not count.',method:'Automatic',repeatable:false},
  {id:'sales-500',name:'Halfway to a Grand',image:'./assets/awards/sales-500.png',meaning:'Five hundred tracked Facebook Marketplace sales — halfway to four digits.',criteria:'Press “I Sold This Vehicle” on 500 Listings, each with its own valid, unique Facebook Marketplace URL. Non-Facebook and reused links do not count.',method:'Automatic',repeatable:false},
  {id:'sales-1000',name:'Grandmaster of the Lot',image:'./assets/awards/sales-1000.png',meaning:'An extraordinary 1,000 tracked Facebook Marketplace vehicle sales.',criteria:'Press “I Sold This Vehicle” on 1,000 Listings, each with its own valid, unique Facebook Marketplace URL. Non-Facebook and reused links do not count.',method:'Automatic',repeatable:false},
  {id:'gold-medal',name:'Gold Medal',image:'./assets/awards/gold-medal.png',meaning:'A first-place Monthly Wrap-Up finish.',criteria:'Total first-place Monthly Wrap-Up finishes.',method:'Automatic',repeatable:true},
  {id:'silver-medal',name:'Silver Medal',image:'./assets/awards/silver-medal.png',meaning:'A second-place Monthly Wrap-Up finish.',criteria:'Total second-place Monthly Wrap-Up finishes.',method:'Automatic',repeatable:true},
  {id:'bronze-medal',name:'Bronze Medal',image:'./assets/awards/bronze-medal.png',meaning:'A third-place Monthly Wrap-Up finish.',criteria:'Total third-place Monthly Wrap-Up finishes.',method:'Automatic',repeatable:true},
  {id:'big-number-1',name:'The Big Number 1',image:'./assets/awards/big-number-1.png',meaning:'Their first-ever first-place Monthly Wrap-Up finish.',criteria:'Finish first in a Monthly Wrap-Up for the first time.',method:'Automatic',repeatable:false},
  {id:'big-runner-up',name:'The Big Runner up',image:'./assets/awards/big-runner-up.png',meaning:'Their first-ever second-place Monthly Wrap-Up finish.',criteria:'Finish second in a Monthly Wrap-Up for the first time.',method:'Automatic',repeatable:false},
  {id:'runner-up-to-runner-up',name:'Runner up to the runner up',image:'./assets/awards/runner-up-to-runner-up.png',meaning:'Their first-ever third-place Monthly Wrap-Up finish.',criteria:'Finish third in a Monthly Wrap-Up for the first time.',method:'Automatic',repeatable:false},
  {id:'you-did-a-thing',name:'You did a thing!',image:'./assets/awards/you-did-a-thing.png',meaning:'They took part and placed within the bottom three of a Monthly Wrap-Up leaderboard.',criteria:'Participate in a Monthly Wrap-Up and place within its bottom three for the first time.',method:'Automatic',repeatable:false},
  {id:'true-achiever',name:'True Achiever',image:'./assets/awards/true-achiever.png',meaning:'Five consecutive Monthly Wrap-Up wins as Top Contributor.',criteria:'Reach five consecutive first-place Monthly Wrap-Up finishes.',method:'Automatic',repeatable:false},
  {id:'mr-over-achiever',name:'Mr.Over-Achiever',image:'./assets/awards/mr-over-achiever.png',meaning:'Ten consecutive Monthly Wrap-Up wins as Top Contributor.',criteria:'Reach ten consecutive first-place Monthly Wrap-Up finishes.',method:'Automatic',repeatable:false},
  {id:'ice-trophy',name:'Huhh? Oh Thanks',image:'./assets/awards/ice-streak.png',meaning:'For receiving the coldest kind of Monthly Wrap-Up recognition.',criteria:'Finish last in three Monthly Wrap-Ups.',method:'Automatic',repeatable:false},
  {id:'iced-iced-baby',name:'Iced Iced Baby',image:'./assets/awards/iced-iced-baby.png',meaning:'This established LotKeys account returned after 30 full inactive days.',criteria:'Return after 30 full inactive days. Temporary recovery progress: 0/3 consecutive calendar days.',method:'Automatic · temporary',repeatable:false,temporary:true},
  {id:'lotkeys-developer',name:'LotKeys Developer',image:'./assets/awards/lotkeys-developer.png',meaning:'Recognized by the LotKeys Creator for helping build and improve LotKeys.',criteria:'Special recognition granted by the registered LotKeys Creator.',method:'Creator awarded',repeatable:false,creatorOnly:true}
];
const byId=new Map(CATALOG.map(x=>[x.id,x]));
const byName=new Map(CATALOG.map(x=>[x.name.toLowerCase(),x]));
let creatorAccess={creatorGoogleAccounts:[],specialAwardIds:['lotkeys-developer']};

const bridge=()=>window.LotKeysMessagingBridge||null;
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const uid=(p='AW')=>`${p}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;
const same=(a,b)=>String(a||'').trim().toLowerCase()===String(b||'').trim().toLowerCase();
const get=(key,fallback)=>bridge()?.getSetting?.(key,fallback)??Promise.resolve(fallback);
const set=(key,value)=>bridge()?.setSetting?.(key,value)??Promise.resolve(false);
const list=v=>Array.isArray(v)?v:[];
const defFor=value=>{if(!value)return null;if(typeof value==='object'&&value.id&&byId.has(value.id))return byId.get(value.id);const raw=typeof value==='string'?value:(value.id||value.label||value.badge||'');return byId.get(raw)||byName.get(String(raw).toLowerCase())||null};

function normalizeEntry(value){
  const def=defFor(value);if(!def)return null;
  const src=typeof value==='object'&&value?value:{};
  const grantIds=[...new Set(list(src.grantIds||src.auditIds).map(String).filter(Boolean))];
  return {id:def.id,label:def.name,count:Math.max(1,Number(src.count)||grantIds.length||1),grantIds,grantedAt:String(src.grantedAt||src.earnedAt||new Date().toISOString()),lastGrantedAt:String(src.lastGrantedAt||src.grantedAt||src.earnedAt||new Date().toISOString()),source:String(src.source||src.method||'Legacy LotKeys award'),storeName:String(src.storeName||''),approvedBy:String(src.approvedBy||''),reason:String(src.reason||'')};
}
function entriesFrom(value){const rows=Array.isArray(value)?value:list(value?.awards||value?.unlockedBadges);return rows.map(normalizeEntry).filter(Boolean)}
function mergeEntries(...collections){const map=new Map();for(const rows of collections)for(const raw of entriesFrom(rows)){const old=map.get(raw.id);if(!old){map.set(raw.id,raw);continue}const ids=[...new Set([...old.grantIds,...raw.grantIds])],count=Math.max(old.count,raw.count,ids.length||0);map.set(raw.id,{...old,...raw,grantedAt:[old.grantedAt,raw.grantedAt].filter(Boolean).sort()[0]||raw.grantedAt,lastGrantedAt:[old.lastGrantedAt,raw.lastGrantedAt].filter(Boolean).sort().slice(-1)[0]||raw.lastGrantedAt,grantIds:ids,count})}return [...map.values()].sort((a,b)=>Date.parse(b.lastGrantedAt||0)-Date.parse(a.lastGrantedAt||0))}
function grantEntry(rows,awardId,auditId,meta={}){const def=byId.get(awardId);if(!def)return {rows:entriesFrom(rows),changed:false};const next=entriesFrom(rows),i=next.findIndex(x=>x.id===awardId),stamp=meta.grantedAt||new Date().toISOString(),audit=String(auditId||uid('GRANT'));if(i>=0){const entry=next[i];if(entry.grantIds.includes(audit)||!def.repeatable)return {rows:next,changed:false};entry.grantIds=[...entry.grantIds,audit];entry.count=Math.max(entry.count+1,entry.grantIds.length);entry.lastGrantedAt=stamp;entry.source=meta.source||entry.source;entry.approvedBy=meta.approvedBy||entry.approvedBy;entry.reason=meta.reason||entry.reason;entry.storeName=meta.storeName||entry.storeName;return {rows:next,changed:true}}next.unshift({id:def.id,label:def.name,count:1,grantIds:[audit],grantedAt:stamp,lastGrantedAt:stamp,source:String(meta.source||def.method),storeName:String(meta.storeName||''),approvedBy:String(meta.approvedBy||''),reason:String(meta.reason||'')});return {rows:next,changed:true}}
function removeEntry(rows,awardId){const before=entriesFrom(rows),after=before.filter(x=>x.id!==awardId);return {rows:after,changed:after.length!==before.length}}
function awardSummary(rows){return entriesFrom(rows).map(x=>({id:x.id,label:x.label,count:x.count,grantIds:x.grantIds,grantedAt:x.grantedAt,lastGrantedAt:x.lastGrantedAt,source:x.source,storeName:x.storeName,approvedBy:x.approvedBy,reason:x.reason}))}

function levelThreshold(level){level=Math.max(1,Math.min(100,Number(level)||1));if(level<=1)return 0;if(level>=100)return 1000;return Math.max(1,Math.ceil(1000*Math.pow((level-1)/99,1.75)))}
function levelForPosts(posts){posts=Math.max(0,Number(posts)||0);if(posts>=1000)return 100;let level=1;for(let n=2;n<=100;n++){if(posts<levelThreshold(n))break;level=n}return level}
function progressState(raw={}){const confirmedMap=new Map(),saleMap=new Map();for(const x of list(raw?.confirmedPosts)){const key=canonicalFacebookMarketplaceUrl(x?.facebookUrl)||String(x?.key||'');if(key&&!confirmedMap.has(key))confirmedMap.set(key,{...x,key})}for(const x of list(raw?.vehicleSales)){const key=canonicalFacebookMarketplaceUrl(x?.facebookUrl);if(key&&!saleMap.has(key))saleMap.set(key,{...x,key,facebookQualified:true})}const confirmed=[...confirmedMap.values()],sales=[...saleMap.values()],level=levelForPosts(confirmed.length);return {schemaVersion:3,confirmedPosts:confirmed.slice(-1500),confirmedPostCount:confirmed.length,vehicleSales:sales.slice(-1500),vehicleSaleCount:sales.length,confirmedFacebookSaleCount:sales.length,level,curve:'progressive-1.75-test',level100Posts:1000,updatedAt:String(raw?.updatedAt||new Date().toISOString())}}
function canonicalFacebookMarketplaceUrl(value=''){
  try{
    const u=new URL(String(value).trim());
    if(u.protocol!=='https:'||!/(^|\.)facebook\.com$/i.test(u.hostname))return'';
    const match=u.pathname.match(/\/marketplace(?:\/[^/]+)?\/item\/(\d+)/i);
    return match?.[1]?`facebook-marketplace:item:${match[1]}`:'';
  }catch{return''}
}
function facebookPostKey(data={}){return canonicalFacebookMarketplaceUrl(data.facebookUrl)}
function validFacebookUrl(value=''){return !!canonicalFacebookMarketplaceUrl(value)}
function localDayKey(value=Date.now()){const d=new Date(value);return Number.isNaN(d.getTime())?'':`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function activityKey(data={},prefix='activity'){const id=String(data.listingId||data.vehicleId||data.id||'').trim();return id?`${prefix}:${id}`:`${prefix}:${String(data.createdAt||data.soldAt||data.deletedAt||Date.now())}`}

function toast(text){const el=document.getElementById('toast');if(el){el.textContent=String(text);el.hidden=false;clearTimeout(toast.timer);toast.timer=setTimeout(()=>{el.hidden=true},2600);return}const t=document.createElement('div');t.className='lkaw-toast';t.textContent=String(text);document.body.appendChild(t);setTimeout(()=>t.remove(),2600)}
function closeModal(){const modal=document.getElementById('modal');modal?.classList.remove('public-profile-modal');if(modal?.open)modal.close()}
function openModal(html){const modal=document.getElementById('modal'),body=document.getElementById('modal-body');if(!modal||!body)return null;body.innerHTML=html;body.querySelector('.lkaw-close')?.addEventListener('click',closeModal);if(!modal.open)modal.showModal();return body}
function definitionOptions({includeCreator=false}={}){return CATALOG.filter(x=>includeCreator||!x.creatorOnly).map(x=>`<option value="${esc(x.id)}">${esc(x.name)} · ${esc(x.method)}</option>`).join('')}
async function currentUser(){const users=list(await get('storeUsers',[])),name=await get('userName',''),email=await get('currentAccountEmail','');return users.find(x=>same(x.userName,name)||(email&&same(x.email,email)))||null}
async function currentAdminLevel(){const user=await currentUser();return Math.max(0,Math.min(2,Number(user?.adminLevel??await get('currentUserAdminLevel',0))||0))}
function permanentUserKey(user={}){return String(user.googleSub||user.accountSub||user.sub||user.accountId||user.email||user.userName||'').trim().toLowerCase()}
async function currentIsCreator(){const email=String(await get('currentAccountEmail','')).trim().toLowerCase(),sub=String(await get('currentAccountSub','')).trim(),allowed=list(creatorAccess.creatorGoogleAccounts).map(x=>String(x).trim().toLowerCase()),binding=await get(CREATOR_BINDING_KEY,{});if(!email||!allowed.includes(email))return false;if(sub&&binding?.googleSub!==sub)await set(CREATOR_BINDING_KEY,{schemaVersion:1,email,googleSub:sub,boundAt:binding?.boundAt||new Date().toISOString(),immutable:true});return true}
async function currentIsDeveloper(){if(await currentIsCreator())return true;const sub=String(await get('currentAccountSub','')).trim(),email=String(await get('currentAccountEmail','')).trim().toLowerCase(),rows=list(await get(DEVELOPER_ACCESS_KEY,[]));return rows.some(x=>x?.active!==false&&((sub&&permanentUserKey(x)===sub)||(email&&(same(x.email,email)||permanentUserKey(x)===email))))}
async function storeName(){return String(await get('storeName','')||await get('storeFolderName','')||'LotKeys Store')}

let syncTimer=null;
function queueAccountSync(){clearTimeout(syncTimer);syncTimer=setTimeout(async()=>{const b=bridge();if(!b)return;try{await set('personalProfileUpdatedAt',Date.now());const status=await b.DriveSync?.getStatus?.();if(status?.connected){await b.DriveSync.syncPersonalProfile?.().catch(console.warn);await b.DriveSync.syncStoreProfileThumbnail?.().catch(console.warn)}}catch(err){console.warn('Awards Account sync deferred',err)}},700)}
async function storeProfileForCurrent(entries,progress,profile){const b=bridge();if(!b)return false;const users=b.normalizeStoreUsers?b.normalizeStoreUsers(await get('storeUsers',[])):list(await get('storeUsers',[])),name=await get('userName',''),email=await get('currentAccountEmail',''),i=users.findIndex(x=>same(x.userName,name)||(email&&same(x.email,email)));if(i<0)return false;const def=byId.get(profile.primaryAwardId),history=list(progress.vehicleSales).filter(x=>x.facebookQualified===true&&x.key).map(x=>({key:x.key,listingId:x.listingId,vehicleId:x.vehicleId,vehicleName:x.vehicleName||'',facebookUrl:x.facebookUrl,postedAt:x.postedAt||'',soldAt:x.soldAt,status:'Sold'})).slice(-1500),next={...users[i],awards:awardSummary(entries),unlockedBadges:awardSummary(entries),primaryAwardId:profile.primaryAwardId||'',displayAwardIds:list(profile.displayAwardIds).slice(0,5),favoriteBadge:def?.name||'',lotLevel:progress.level,confirmedFacebookPosts:progress.confirmedPosts.length,lotKeysVehicleSales:progress.vehicleSales.length,confirmedFacebookSales:progress.confirmedFacebookSaleCount,facebookSaleHistory:history,awardProfile:{primaryAwardId:profile.primaryAwardId||'',displayAwardIds:list(profile.displayAwardIds).slice(0,5)}};if(JSON.stringify(users[i])===JSON.stringify(next))return false;users[i]=b.normalizeUserAccount?b.normalizeUserAccount(next):next;await set('storeUsers',users);return true}
async function localProfile(entries=null){entries=entries||entriesFrom(await get('unlockedBadges',[]));const raw=await get(PROFILE_KEY,{}),valid=new Set(entries.map(x=>x.id));let primary=String(raw?.primaryAwardId||'');if(!valid.has(primary)){const legacy=defFor(await get('favoriteBadge',''));primary=legacy&&valid.has(legacy.id)?legacy.id:(entries[0]?.id||'')}let display=list(raw?.displayAwardIds).map(String).filter((x,i,a)=>valid.has(x)&&x!==primary&&a.indexOf(x)===i).slice(0,5);for(const e of entries)if(e.id!==primary&&!display.includes(e.id)&&display.length<5)display.push(e.id);return {schemaVersion:1,primaryAwardId:primary,displayAwardIds:display,updatedAt:String(raw?.updatedAt||new Date().toISOString())}}
async function importCurrentStoreAwards(){const me=await currentUser(),local=entriesFrom(await get('unlockedBadges',[])),remote=entriesFrom(me?.awards?.length?me.awards:me?.unlockedBadges),merged=mergeEntries(local,remote),profile=await localProfile(merged),oldProfile=await get(PROFILE_KEY,{});let changed=JSON.stringify(local)!==JSON.stringify(merged)||JSON.stringify({primaryAwardId:oldProfile?.primaryAwardId||'',displayAwardIds:list(oldProfile?.displayAwardIds)})!==JSON.stringify({primaryAwardId:profile.primaryAwardId,displayAwardIds:profile.displayAwardIds});if(changed){profile.updatedAt=new Date().toISOString();await set('unlockedBadges',awardSummary(merged));await set(PROFILE_KEY,profile);await set('favoriteBadge',byId.get(profile.primaryAwardId)?.name||'');queueAccountSync()}return {entries:merged,profile}}
async function reconcileAccountAccess(){const imported=await importCurrentStoreAwards();if(await currentIsCreator())await grantCurrentAutomatic('lotkeys-developer','creator:permanent','Registered LotKeys Creator');return imported}

async function ensureProgressFromListings(){const b=bridge();if(!b?.DB)return progressState(await get(PROGRESS_KEY,{}));const state=progressState(await get(PROGRESS_KEY,{})),seen=new Set(state.confirmedPosts.map(x=>x.key)),myName=String(await get('userName','')),rows=await b.DB.all('listings'),added=[];for(const row of rows){if(!row?.postedAt||!validFacebookUrl(row.facebookUrl)||row.ownerUserName&&!same(row.ownerUserName,myName))continue;const key=facebookPostKey(row);if(!key||seen.has(key))continue;seen.add(key);added.push({key,listingId:String(row.id||''),vehicleId:String(row.vehicleId||''),facebookUrl:String(row.facebookUrl||''),postedAt:row.postedAt,confirmedAt:new Date().toISOString(),source:'Existing saved LotKeys Listing'})}if(!added.length)return state;const next=progressState({...state,confirmedPosts:[...state.confirmedPosts,...added],updatedAt:new Date().toISOString()});await set(PROGRESS_KEY,next);const imported=await importCurrentStoreAwards(),profile=imported.profile;await storeProfileForCurrent(imported.entries,next,profile);queueAccountSync();return next}
async function confirmFacebookPost(data={}){try{if(!validFacebookUrl(data.facebookUrl))return false;const myName=String(await get('userName',''));if(data.ownerUserName&&!same(data.ownerUserName,myName))return false;const state=progressState(await get(PROGRESS_KEY,{})),key=facebookPostKey(data);if(!key||state.confirmedPosts.some(x=>x.key===key))return false;const previous=state.level;state.confirmedPosts.push({key,listingId:String(data.listingId||''),vehicleId:String(data.vehicleId||''),facebookUrl:String(data.facebookUrl||''),postedAt:data.postedAt||Date.now(),confirmedAt:new Date().toISOString(),source:String(data.source||'LotKeys confirmed Facebook post')});state.updatedAt=new Date().toISOString();const next=progressState(state);await set(PROGRESS_KEY,next);const imported=await importCurrentStoreAwards();await storeProfileForCurrent(imported.entries,next,imported.profile);const vehicle=data.vehicleId?await bridge()?.DB?.get?.('vehicles',data.vehicleId):null;if(Number(data.price||vehicle?.price)>0&&String(vehicle?.websitePriceFinding?.status||'')==='call-for-price')await grantCurrentAutomatic('faster-as-f-boy',`auto:faster:${key}`,'The source-page check explicitly found a non-price placeholder before this numeric Facebook Listing was created');queueAccountSync();if(next.level>previous)toast(`Lot-Lvl ${next.level} unlocked ⚡`);scheduleDecorate();return true}catch(err){console.warn('Lot-Lvl confirmation deferred',err);return false}}

async function recordVehicleProfileCreated(data={}){try{return await grantCurrentAutomatic('new-kid-on-the-lot',`auto:first-profile:${String(data.vehicleId||data.id||'first')}`,'Created a first Vehicle Profile')}catch(err){console.warn('New Kid on the Lot award deferred',err);return false}}
async function recordListingCreated(data={}){try{const created=new Date(data.createdAt||Date.now()),hour=created.getHours(),minute=created.getMinutes();if(!([4,16].includes(hour)&&minute>=19&&minute<=21))return false;return await grantCurrentAutomatic('dude-wheres-my-car',`auto:dude:${activityKey(data,'listing')}`,'Created a Listing during the hidden 4:19–4:21 window')}catch(err){console.warn('Dude Where’s My Car award deferred',err);return false}}
async function recordListingDeleted(data={}){try{const website=String(data.websiteUrl||data.originalListingUrl||'').trim();if(!/^https?:\/\//i.test(website))return false;return await grantCurrentAutomatic('anti-celibratory',`auto:anti:${activityKey(data,'listing')}`,'Deleted a website-connected Listing without marking it sold')}catch(err){console.warn('Anti-Celibration award deferred',err);return false}}
async function validateVehicleSale(data={}){
  const myName=String(await get('userName',''));
  if(data.ownerUserName&&!same(data.ownerUserName,myName))return {ok:false,reason:'Only the owner of this Listing can mark it sold.'};
  const key=canonicalFacebookMarketplaceUrl(data.facebookUrl);
  if(!key)return {ok:false,reason:'Add this Listing’s valid Facebook Marketplace item URL before using I Sold This Vehicle.'};
  const state=progressState(await get(PROGRESS_KEY,{}));
  if(state.vehicleSales.some(x=>x.key===key))return {ok:false,reason:'That Facebook Marketplace link has already been counted as sold.'};
  const users=list(await get('storeUsers',[]));
  if(users.some(user=>list(user.facebookSaleHistory).some(x=>(canonicalFacebookMarketplaceUrl(x?.facebookUrl)||String(x?.key||''))===key)))return {ok:false,reason:'That Facebook Marketplace link was already used for a Store sale.'};
  return {ok:true,key};
}
async function recordVehicleSale(data={}){
  try{
    const myName=String(await get('userName','')),myEmail=String(await get('currentAccountEmail','')),validation=await validateVehicleSale(data);if(!validation.ok)return false;
    const state=progressState(await get(PROGRESS_KEY,{})),key=validation.key;
    const soldAt=data.soldAt||Date.now(),row={key,listingId:String(data.listingId||data.id||''),vehicleId:String(data.vehicleId||''),vehicleName:String(data.vehicleName||''),facebookUrl:String(data.facebookUrl||''),facebookQualified:true,postedAt:data.postedAt||'',soldAt,dayKey:localDayKey(soldAt),source:'I Sold This Vehicle'};
    state.vehicleSales.push(row);state.updatedAt=new Date().toISOString();const next=progressState(state);await set(PROGRESS_KEY,next);
    const milestones=[[1,'woop-woop'],[10,'mr-sales-man'],[50,'sales-50'],[100,'sales-100'],[250,'quarter-k-club'],[500,'sales-500'],[1000,'sales-1000']];for(const [count,awardId] of milestones)if(next.confirmedFacebookSaleCount>=count)await grantCurrentAutomatic(awardId,`auto:facebook-sales:${count}`,`${count} unique LotKeys-assisted Facebook vehicle sale${count===1?'':'s'}`);
    const sameDay=next.vehicleSales.filter(x=>x.dayKey===row.dayKey),nominations=list(await get(NOMINATIONS_KEY,[])),identity=(myEmail||myName).toLowerCase(),queueReview=(id,awardId,reason)=>{if(nominations.some(x=>x.id===id))return false;nominations.push({id,kind:'automatic-admin-review',status:'pending',awardId,targetUserName:myName,requestedByUserName:'LotKeys',reason,createdAt:new Date().toISOString()});return true};let queued=false;if(next.confirmedFacebookSaleCount===1&&myName)queued=queueReview(`auto:tie-cut:${identity}`,'no-newbie','First verified Facebook Marketplace vehicle sale recorded through I Sold This Vehicle.')||queued;if(sameDay.length===2&&myName)queued=queueReview(`auto:cherrys:${row.dayKey}:${identity}`,'cherrys',`Two verified Facebook Marketplace vehicle sales recorded on ${row.dayKey}.`)||queued;if(sameDay.length===3&&myName)queued=queueReview(`auto:hat-trick:${row.dayKey}:${identity}`,'hat-trick',`Three verified Facebook Marketplace vehicle sales recorded on ${row.dayKey}.`)||queued;if(queued){await set(NOMINATIONS_KEY,nominations);await bridge()?.DriveSync?.syncStoreConfig?.().catch(err=>console.warn('Sales award review will sync later',err));toast(sameDay.length>=3?'Hat Trick sent for Admin review 🎩':sameDay.length===2?'Cherry’s sent for Admin review 🍒':'First sale sent for Tie Cut! review ✂️')}
    const imported=await importCurrentStoreAwards();await storeProfileForCurrent(imported.entries,next,imported.profile);queueAccountSync();scheduleDecorate();return true;
  }catch(err){console.warn('Vehicle sale award tracking deferred',err);return false}
}

async function grantCurrentAutomatic(awardId,auditId,reason=''){const imported=await importCurrentStoreAwards(),result=grantEntry(imported.entries,awardId,auditId,{source:'Automatic',reason,storeName:await storeName()});if(!result.changed)return false;await set('unlockedBadges',awardSummary(result.rows));const profile=await localProfile(result.rows);await set(PROFILE_KEY,profile);await set('favoriteBadge',byId.get(profile.primaryAwardId)?.name||'');const progress=progressState(await get(PROGRESS_KEY,{}));await storeProfileForCurrent(result.rows,progress,profile);queueAccountSync();return true}
async function removeCurrentAutomatic(awardId){const imported=await importCurrentStoreAwards(),result=removeEntry(imported.entries,awardId);if(!result.changed)return false;await set('unlockedBadges',awardSummary(result.rows));const profile=await localProfile(result.rows);await set(PROFILE_KEY,profile);await set('favoriteBadge',byId.get(profile.primaryAwardId)?.name||'');const progress=progressState(await get(PROGRESS_KEY,{}));await storeProfileForCurrent(result.rows,progress,profile);queueAccountSync();return true}

async function saveStoreUsers(users,{sync=true}={}){const b=bridge();await set('storeUsers',b?.normalizeStoreUsers?b.normalizeStoreUsers(users):users);if(sync)await b?.DriveSync?.syncStoreConfig?.();return users}
function grantUserRecord(user,awardId,auditId,meta={}){const current=entriesFrom(user?.awards?.length?user.awards:user?.unlockedBadges),result=grantEntry(current,awardId,auditId,meta);if(!result.changed)return {user,changed:false};return {user:{...user,awards:awardSummary(result.rows),unlockedBadges:awardSummary(result.rows)},changed:true}}
async function grantStoreAward(userIdentifier,awardId,auditId,meta={},sync=true){if(list(creatorAccess.specialAwardIds).includes(String(awardId))&&!await currentIsCreator())throw new Error('Only the registered LotKeys Creator can approve this special Award.');const users=list(await get('storeUsers',[])),needle=String(userIdentifier||'').trim().toLowerCase(),i=users.findIndex(x=>permanentUserKey(x)===needle||same(x.userName,userIdentifier)||same(x.email,userIdentifier));if(i<0)throw new Error('That LotKeys Store account could not be found.');const result=grantUserRecord(users[i],awardId,auditId,{...meta,storeName:meta.storeName||await storeName()});if(!result.changed)return false;users[i]=result.user;const issuer=await currentUser(),audit=list(await get(SPECIAL_AUDIT_KEY,[]));audit.push({id:String(auditId||uid('GRANT')),awardId,recipientAccountId:permanentUserKey(users[i]),recipientUserName:String(users[i].userName||''),issuerAccountId:permanentUserKey(issuer||{}),issuerUserName:String(issuer?.userName||await get('userName','')),timestamp:new Date().toISOString(),reason:String(meta.reason||''),source:String(meta.source||'LotKeys award grant')});await set(SPECIAL_AUDIT_KEY,audit.slice(-2000));if(awardId==='lotkeys-developer'&&await currentIsCreator()){const access=list(await get(DEVELOPER_ACCESS_KEY,[])),key=permanentUserKey(users[i]);if(key&&!access.some(x=>permanentUserKey(x)===key)){access.push({accountId:key,googleSub:String(users[i].googleSub||users[i].accountSub||users[i].sub||''),email:String(users[i].email||''),userName:String(users[i].userName||''),active:true,authorizedAt:new Date().toISOString(),authorizedBy:permanentUserKey(issuer||{})});await set(DEVELOPER_ACCESS_KEY,access)}}await saveStoreUsers(users,{sync});if(permanentUserKey(users[i])===permanentUserKey(await currentUser()||{})||same(users[i].userName,await get('userName','')))await importCurrentStoreAwards();return true}

async function recordApprovedProfileCorrection(userName,requestId){try{if(!userName||!requestId)return false;const users=list(await get('storeUsers',[])),i=users.findIndex(x=>same(x.userName,userName));if(i<0)return false;const stats=users[i].awardStats&&typeof users[i].awardStats==='object'?{...users[i].awardStats}:{},ids=[...new Set(list(stats.approvedProfileCorrectionIds).map(String))];if(ids.includes(String(requestId)))return false;ids.push(String(requestId));stats.approvedProfileCorrectionIds=ids.slice(-500);stats.approvedProfileCorrections=ids.length;users[i]={...users[i],awardStats:stats};let changed=true;if(ids.length>=10){const r=grantUserRecord(users[i],'detail-detective','auto:profile-corrections:10',{source:'Automatic',reason:'10 approved Vehicle Profile correction requests',storeName:await storeName()});users[i]=r.user;changed=changed||r.changed}if(ids.length>=100){const r=grantUserRecord(users[i],'folder-freak','auto:profile-corrections:100',{source:'Automatic',reason:'100 approved Vehicle Profile correction requests',storeName:await storeName()});users[i]=r.user;changed=changed||r.changed}if(changed){await saveStoreUsers(users);if(same(userName,await get('userName','')))await importCurrentStoreAwards()}scheduleDecorate();return true}catch(err){console.warn('Profile-correction award deferred',err);return false}}

function standingRows(snapshot={}){return list(snapshot.standings).filter(x=>Number(x.points)>0).slice().sort((a,b)=>(Number(a.placement)||999)-(Number(b.placement)||999)||Number(b.points)-Number(a.points))}
async function applyAutomaticStoreAwards(){
  try{
    if(await currentAdminLevel()<1)return false;
    const history=list(await get('monthlyContributionHistory',[])).filter(x=>x?.monthKey&&Array.isArray(x.standings)).slice().sort((a,b)=>(Number(a.monthStart)||Date.parse(a.monthKey)||0)-(Number(b.monthStart)||Date.parse(b.monthKey)||0));
    const users=list(await get('storeUsers',[]));
    if(!history.length||!users.length)return false;
    const activeStoreName=await storeName(),winRuns=new Map(),lastCounts=new Map();
    let changed=false;
    const give=(user,awardId,auditId)=>grantUserRecord(user,awardId,auditId,{source:'Automatic Monthly Wrap-Up',storeName:activeStoreName});
    for(const snap of history){
      const rows=standingRows(snap);
      if(!rows.length)continue;
      const placements=new Map(rows.map((r,i)=>[String(r.userName||'').toLowerCase(),Number(r.placement)||i+1]));
      const lastName=String(rows[rows.length-1].userName||'');
      for(let i=0;i<users.length;i++){
        const name=String(users[i].userName||''),key=name.toLowerCase(),place=placements.get(key)||0;
        const grant=(awardId,auditId)=>{const result=give(users[i],awardId,auditId);users[i]=result.user;changed=changed||result.changed};
        if(place===1){grant('gold-medal',`auto:gold:${snap.monthKey}`);grant('big-number-1',`auto:first:${snap.monthKey}`)}
        else if(place===2){grant('silver-medal',`auto:silver:${snap.monthKey}`);grant('big-runner-up',`auto:second:${snap.monthKey}`)}
        else if(place===3){grant('bronze-medal',`auto:bronze:${snap.monthKey}`);grant('runner-up-to-runner-up',`auto:third:${snap.monthKey}`)}
        if(place&&place>Math.max(0,rows.length-3))grant('you-did-a-thing',`auto:bottom-three:${snap.monthKey}`);
        const wins=place===1?(winRuns.get(key)||0)+1:0;
        winRuns.set(key,wins);
        if(wins>=5)grant('true-achiever',`auto:five-wins:${snap.monthKey}`);
        if(wins>=10)grant('mr-over-achiever',`auto:ten-wins:${snap.monthKey}`);
        const lastCount=(lastCounts.get(key)||0)+(same(name,lastName)?1:0);
        lastCounts.set(key,lastCount);
        if(lastCount>=3)grant('ice-trophy','auto:last-place:three');
      }
    }
    if(changed){await saveStoreUsers(users);await importCurrentStoreAwards();scheduleDecorate()}
    return changed;
  }catch(err){console.warn('Automatic Store awards deferred',err);return false}
}

async function processCheckIn(){try{const raw=await get(CHECKIN_KEY,{}),now=new Date(),today=Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),now.getUTCDate()),last=Date.parse(raw?.lastCheckInAt||0)||0,lastDate=last?new Date(last):null,lastDay=lastDate?Date.UTC(lastDate.getUTCFullYear(),lastDate.getUTCMonth(),lastDate.getUTCDate()):0,accountCreated=Date.parse(await get('accountCreatedAt',''))||0,storeConnected=!!(await get('storeFolderId','')),established=!!(accountCreated&&storeConnected&&today-Date.UTC(new Date(accountCreated).getUTCFullYear(),new Date(accountCreated).getUTCMonth(),new Date(accountCreated).getUTCDate())>=30*86400000),returnedAfter30=!!(established&&lastDay&&today-lastDay>=30*86400000);let inactiveAwardActive=raw?.inactiveAwardActive===true||returnedAfter30;if(lastDay===today)return;if(returnedAfter30)await grantCurrentAutomatic('iced-iced-baby',`auto:inactive:${today}`,'Returned after 30 full inactive calendar days');else if(!inactiveAwardActive)await removeCurrentAutomatic('iced-iced-baby');let streak=inactiveAwardActive?(lastDay===today-86400000?Math.max(1,Number(raw?.consecutiveDays)||0)+1:1):0;if(inactiveAwardActive&&streak>=3){await removeCurrentAutomatic('iced-iced-baby');inactiveAwardActive=false}await set(CHECKIN_KEY,{schemaVersion:3,lastCheckInAt:new Date().toISOString(),consecutiveDays:streak,inactiveAwardActive,recoveryProgress:inactiveAwardActive?Math.min(3,streak):3,updatedAt:new Date().toISOString()});queueAccountSync()}catch(err){console.warn('Award check-in tracking deferred',err)}}

function countLabel(entry){const def=byId.get(entry.id),count=Math.max(1,Number(entry?.count)||1);return def?.repeatable?(count>=100?'99+':String(count)):''}
function badgeButton(entry,{large=false,primary=false,reorder=false}={}){const def=byId.get(entry.id);if(!def)return'';const count=countLabel(entry);return `<button class="lkaw-badge ${large?'large':''} ${primary?'primary':''} ${reorder?'lkaw-reorder-badge':''}" type="button" data-lkaw-id="${esc(entry.id)}" ${reorder?'data-lkaw-reorder="1" draggable="false"':''} title="${esc(def.name)}" aria-label="${esc(def.name)}${count?`, received ${entry.count} times`:''}"><span class="lkaw-art"><img src="${esc(def.image)}" alt="" draggable="false">${count?`<b>${esc(count)}</b>`:''}${reorder?'<i class="lkaw-reorder-cue" aria-hidden="true">↕</i>':''}</span><span>${esc(def.name)}</span></button>`}
function emptyBadge(){return '<span class="lkaw-empty-badge"><i>＋</i><small>Open slot</small></span>'}
function bindBadgeButtons(root,entries){root?.querySelectorAll?.('[data-lkaw-id]:not([data-lkaw-reorder])').forEach(btn=>{let timer=null;btn.addEventListener('contextmenu',e=>e.preventDefault());btn.addEventListener('pointerdown',()=>{timer=setTimeout(()=>showAwardName(btn,byId.get(btn.dataset.lkawId)?.name||'LotKeys Award'),550)});['pointerup','pointercancel','pointerleave'].forEach(ev=>btn.addEventListener(ev,()=>clearTimeout(timer)));btn.addEventListener('click',()=>showAwardDetails(btn.dataset.lkawId,entries.find(x=>x.id===btn.dataset.lkawId)))})}
function showAwardName(btn,name){document.querySelector('.lkaw-name-tip')?.remove();const tip=document.createElement('div');tip.className='lkaw-name-tip';tip.textContent=name;document.body.appendChild(tip);const r=btn.getBoundingClientRect();tip.style.left=`${Math.max(8,Math.min(innerWidth-tip.offsetWidth-8,r.left+r.width/2-tip.offsetWidth/2))}px`;tip.style.top=`${Math.max(8,r.top-tip.offsetHeight-8)}px`;setTimeout(()=>tip.remove(),1300)}
async function showAwardDetails(id,entry){const def=byId.get(id);if(!def)return;let criteria=def.criteria;if(id==='iced-iced-baby'){const check=await get(CHECKIN_KEY,{}),progress=check?.inactiveAwardActive?Math.max(0,Math.min(3,Number(check.recoveryProgress??check.consecutiveDays)||0)):3;criteria=`Return after 30 full inactive days. Recovery progress: ${progress}/3 consecutive calendar days${progress>=3?' — recovered.':' before this temporary award is removed.'}`}const total=def.repeatable||Number(entry?.count)>1?`<div class="lkaw-total">Total received: ${Math.max(1,Number(entry?.count)||1)}</div>`:'';openModal(`<div class="modal-head"><div><div class="eyebrow">LOTKEYS AWARD</div><h2>${esc(def.name)}</h2></div><button class="close lkaw-close">×</button></div><div class="lkaw-detail"><img src="${esc(def.image)}" alt="${esc(def.name)}"><p>${esc(def.meaning)}</p>${def.hiddenCriteria?'':`<div class="lkaw-criteria"><strong>How it unlocks</strong><span>${esc(criteria)}</span></div>`}${total}<small>Earned ${entry?.lastGrantedAt?esc(new Date(entry.lastGrantedAt).toLocaleDateString('en-CA')):'through LotKeys'}</small></div>`)}

async function renderProfileCard(){const main=document.getElementById('main'),identity=main?.querySelector('.profile-identity-card');if(!identity||document.getElementById('lkaw-profile-card'))return;const imported=await importCurrentStoreAwards(),progress=await ensureProgressFromListings(),entries=imported.entries,profile=await localProfile(entries),primary=entries.find(x=>x.id===profile.primaryAwardId),display=profile.displayAwardIds.map(id=>entries.find(x=>x.id===id)).filter(Boolean).slice(0,5),level=progress.level,currentAt=levelThreshold(level),nextAt=level>=100?1000:levelThreshold(level+1),pct=level>=100?100:Math.max(0,Math.min(100,Math.round(100*(progress.confirmedPosts.length-currentAt)/Math.max(1,nextAt-currentAt)))),remaining=Math.max(0,nextAt-progress.confirmedPosts.length);const card=document.createElement('div');card.className='card lkaw-profile-card';card.id='lkaw-profile-card';card.innerHTML=`<div class="lkaw-title"><div><div class="eyebrow">LOTKEYS AWARDS</div><h2>Lot-Lvl ${level}</h2></div><strong>${progress.confirmedPosts.length.toLocaleString('en-CA')} / 1,000</strong></div><div class="lkaw-level-track"><i style="width:${pct}%"></i></div><div class="lkaw-level-note">${level>=100?'Maximum Lot-Lvl reached ⚡':`${remaining} more confirmed Facebook vehicle post${remaining===1?'':'s'} to Lot-Lvl ${level+1}`}</div><div class="lkaw-showcase"><div class="lkaw-primary"><small>Primary Award</small>${primary?badgeButton(primary,{large:true,primary:true}):emptyBadge()}</div><div class="lkaw-five"><small>Five Displayed Awards</small><div>${[0,1,2,3,4].map(i=>display[i]?badgeButton(display[i]):emptyBadge()).join('')}</div></div></div><div class="actions lkaw-actions"><button class="btn" id="lkaw-customize" type="button" ${entries.length?'':'disabled'}>Choose Display</button><button class="btn ghost" id="lkaw-view-all" type="button" ${entries.length?'':'disabled'}>View All User Awards</button><button class="btn primary" id="lkaw-request" type="button">Request an Award</button></div><div class="help">Awards and Lot-Lvl stay with your personal LotKeys Account when you change Stores. The current test curve reaches Level 100 at 1,000 confirmed Facebook vehicle posts.</div>`;identity.after(card);identity.querySelector('#favorite-badge')?.closest?.('.field')?.setAttribute('hidden','');card.querySelector('#lkaw-customize')?.addEventListener('click',()=>showDisplayManager(entries,profile));card.querySelector('#lkaw-view-all')?.addEventListener('click',()=>showAllAwards(entries));card.querySelector('#lkaw-request')?.addEventListener('click',showAwardRequest);bindBadgeButtons(card,entries);decorateInlinePrimary(document)}

function showAllAwards(entries){const body=openModal(`<div class="modal-head"><div><div class="eyebrow">MY LOTKEYS ACCOUNT</div><h2>All User Awards</h2></div><button class="close lkaw-close">×</button></div><div class="lkaw-all-grid">${entries.length?entries.map(x=>badgeButton(x)).join(''):'<div class="empty">No awards earned yet.</div>'}</div>`);bindBadgeButtons(body,entries)}
function showDisplayManager(entries,profile){
  const rows=entries.map(e=>{const def=byId.get(e.id);return `<div class="lkaw-picker-row"><img src="${esc(def.image)}" alt=""><span><strong>${esc(def.name)}</strong><small>${esc(def.meaning)}</small></span><label><input type="radio" name="lkaw-primary-choice" value="${esc(e.id)}" ${profile.primaryAwardId===e.id?'checked':''}> Primary</label><label><input type="checkbox" data-lkaw-display-choice value="${esc(e.id)}" ${profile.displayAwardIds.includes(e.id)?'checked':''}> Display</label></div>`}).join('');
  const body=openModal(`<div class="modal-head"><div><div class="eyebrow">MY LOTKEYS ACCOUNT</div><h2>Choose Displayed Awards</h2></div><button class="close lkaw-close">×</button></div><div class="help">Choose one Primary Award and up to five additional awards. Drag any displayed badge itself to change the order.</div><div class="lkaw-display-order"><small>DISPLAY ORDER</small><div id="lkaw-order-strip"></div></div><div class="lkaw-picker-list">${rows}</div><button class="btn primary block" id="lkaw-save-display" type="button">Save Award Display</button>`);
  let order=list(profile.displayAwardIds).filter(id=>entries.some(e=>e.id===id)).slice(0,5),drag=null;
  const selected=()=>[...body.querySelectorAll('[data-lkaw-display-choice]:checked')].map(x=>x.value);
  const renderOrder=()=>{const chosen=selected();order=order.filter(id=>chosen.includes(id));for(const id of chosen)if(!order.includes(id)&&order.length<5)order.push(id);const strip=body.querySelector('#lkaw-order-strip');strip.innerHTML=order.map(id=>badgeButton(entries.find(e=>e.id===id),{reorder:true})).join('')||'<div class="help">Choose Display on up to five awards.</div>';strip.querySelectorAll('[data-lkaw-reorder]').forEach(btn=>{btn.addEventListener('contextmenu',e=>e.preventDefault());btn.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'&&e.button!==0)return;drag={id:btn.dataset.lkawId,pointerId:e.pointerId};btn.setPointerCapture?.(e.pointerId);btn.classList.add('dragging');e.preventDefault()});btn.addEventListener('pointermove',e=>{if(!drag||drag.pointerId!==e.pointerId)return;const over=document.elementFromPoint(e.clientX,e.clientY)?.closest?.('[data-lkaw-reorder]');if(!over||over===btn)return;const from=order.indexOf(drag.id),to=order.indexOf(over.dataset.lkawId);if(from<0||to<0)return;order.splice(to,0,order.splice(from,1)[0]);if(to>from)strip.insertBefore(btn,over.nextSibling);else strip.insertBefore(btn,over)});const end=e=>{if(!drag||drag.pointerId!==e.pointerId)return;drag=null;btn.classList.remove('dragging')};btn.addEventListener('pointerup',end);btn.addEventListener('pointercancel',end)});};
  body.querySelectorAll('[data-lkaw-display-choice]').forEach(x=>x.addEventListener('change',()=>{const checked=selected();if(checked.length>5){x.checked=false;toast('Choose up to five displayed awards.')}renderOrder()}));
  body.querySelectorAll('[name="lkaw-primary-choice"]').forEach(x=>x.addEventListener('change',()=>{const duplicate=body.querySelector(`[data-lkaw-display-choice][value="${CSS.escape(x.value)}"]`);if(duplicate?.checked){duplicate.checked=false;order=order.filter(id=>id!==x.value);renderOrder()}}));
  renderOrder();
  body.querySelector('#lkaw-save-display').onclick=async()=>{const save=body.querySelector('#lkaw-save-display'),primary=String(body.querySelector('[name="lkaw-primary-choice"]:checked')?.value||''),display=order.filter(x=>x!==primary).slice(0,5),next={schemaVersion:1,primaryAwardId:primary,displayAwardIds:display,updatedAt:new Date().toISOString()};save.disabled=true;save.textContent='Saving…';await set(PROFILE_KEY,next);await set('favoriteBadge',byId.get(primary)?.name||'');const progress=progressState(await get(PROGRESS_KEY,{}));await storeProfileForCurrent(entries,progress,next);closeModal();await bridge()?.rerender?.();document.getElementById('lkaw-profile-card')?.remove();await renderProfileCard();decorateInlinePrimary(document);queueAccountSync();toast('Award display saved ✓')};
}

async function showAwardRequest(){const name=String(await get('userName',''));if(!name)return toast('Connect to a LotKeys Store before requesting an award.');const body=openModal(`<div class="modal-head"><div><div class="eyebrow">AWARD REQUEST</div><h2>Request an Award</h2></div><button class="close lkaw-close">×</button></div><div class="field"><label>Award</label><select id="lkaw-request-award">${definitionOptions()}</select></div><div class="field"><label>Why should Administration review this?</label><textarea id="lkaw-request-reason" maxlength="500" placeholder="Describe the LotKeys activity or milestone…"></textarea></div><button class="btn primary block" id="lkaw-submit-request" type="button">Send to Store Administration</button><div class="help">The public badge meaning does not reveal hidden qualification details. Store Administration makes the final decision.</div>`);body.querySelector('#lkaw-submit-request').onclick=async()=>{const awardId=body.querySelector('#lkaw-request-award').value,reason=body.querySelector('#lkaw-request-reason').value.trim();if(!reason)return toast('Add a short reason for Administration.');const rows=list(await get(NOMINATIONS_KEY,[]));rows.push({id:uid('REQ'),kind:'user-request',status:'pending',awardId,targetUserName:name,requestedByUserName:name,reason:reason.slice(0,500),createdAt:new Date().toISOString()});await set(NOMINATIONS_KEY,rows);await bridge()?.DriveSync?.syncStoreConfig?.().catch(console.warn);closeModal();toast('Award request sent ✓');scheduleDecorate()}}

async function renderGarageCard(){const main=document.getElementById('main');if(!main||document.getElementById('lkaw-garage-card')||String(document.getElementById('page-title')?.textContent)!=='Garage')return;const level=await currentAdminLevel();if(level<1)return;const rows=list(await get(NOMINATIONS_KEY,[])),pending=rows.filter(x=>x.status==='pending'),currentName=String(await get('userName','')),mine=pending.filter(x=>same(x.requestedByUserName,currentName));const card=document.createElement('div');card.className='card lkaw-garage-card';card.id='lkaw-garage-card';card.innerHTML=`<div class="section-title" style="margin-top:0"><div><h2>${level>=2?'🌟':'⭐'} Awards Administration</h2><small>${pending.length} pending review</small></div></div><div class="lkaw-admin-stats"><div><strong>${CATALOG.length}</strong><span>Store Award Definitions</span></div><div><strong>${level>=2?pending.length:mine.length}</strong><span>${level>=2?'Pending Requests':'My Nominations'}</span></div></div><div class="actions"><button class="btn primary" id="lkaw-nominate" type="button">Nominate User</button>${level>=2?'<button class="btn" id="lkaw-direct" type="button">Direct Grant</button><button class="btn ghost" id="lkaw-review" type="button">Review Queue</button><button class="btn ghost" id="lkaw-auto" type="button">Recheck Automatic Awards</button>':''}</div><div class="help">Admin Level 1 nominates with a reason. Admin Level 2 approves or denies requests and can directly grant any Store award.</div>`;const reports=document.getElementById('user-reports-card');if(reports)reports.before(card);else main.appendChild(card);card.querySelector('#lkaw-nominate').onclick=()=>showAdminGrant(false);card.querySelector('#lkaw-direct')?.addEventListener('click',()=>showAdminGrant(true));card.querySelector('#lkaw-review')?.addEventListener('click',showReviewQueue);card.querySelector('#lkaw-auto')?.addEventListener('click',async()=>{const b=card.querySelector('#lkaw-auto');b.disabled=true;b.textContent='Checking…';const changed=await applyAutomaticStoreAwards();b.disabled=false;b.textContent='Recheck Automatic Awards';toast(changed?'Automatic awards updated ✓':'Automatic awards already current')})}

async function showAdminGrant(direct=false){const level=await currentAdminLevel();if(level<1 || direct&&level<2)return;const users=list(await get('storeUsers',[])).filter(x=>x.status!=='disabled'),me=String(await get('userName','')),includeCreator=direct&&await currentIsCreator(),body=openModal(`<div class="modal-head"><div><div class="eyebrow">${direct?'ADMIN LEVEL 2':'AWARD NOMINATION'}</div><h2>${direct?'Directly Grant Award':'Nominate a User'}</h2></div><button class="close lkaw-close">×</button></div><div class="field"><label>LotKeys Store User</label><select id="lkaw-admin-user">${users.map(x=>`<option value="${esc(x.userName)}">${esc(x.profileDisplayName||x.userName)}</option>`).join('')}</select></div><div class="field"><label>Award</label><select id="lkaw-admin-award">${definitionOptions({includeCreator})}</select></div><div class="field"><label>Reason</label><textarea id="lkaw-admin-reason" maxlength="500" placeholder="Reason for this ${direct?'grant':'nomination'}…"></textarea></div><button class="btn primary block" id="lkaw-admin-submit" type="button">${direct?'Grant Award':'Submit Nomination'}</button>`);body.querySelector('#lkaw-admin-submit').onclick=async()=>{const target=body.querySelector('#lkaw-admin-user').value,awardId=body.querySelector('#lkaw-admin-award').value,reason=body.querySelector('#lkaw-admin-reason').value.trim();if(!reason)return toast('Add a reason for the award record.');const btn=body.querySelector('#lkaw-admin-submit');btn.disabled=true;btn.textContent='Saving…';try{if(direct){await grantStoreAward(target,awardId,uid('DIRECT'),{source:'Admin Level 2 direct grant',approvedBy:me,reason});toast('Award granted ✓')}else{const rows=list(await get(NOMINATIONS_KEY,[]));rows.push({id:uid('NOM'),kind:'admin-nomination',status:'pending',awardId,targetUserName:target,requestedByUserName:me,reason:reason.slice(0,500),createdAt:new Date().toISOString()});await set(NOMINATIONS_KEY,rows);await bridge()?.DriveSync?.syncStoreConfig?.();toast('Nomination sent for Admin Level 2 review ✓')}closeModal();scheduleDecorate()}catch(err){btn.disabled=false;btn.textContent=direct?'Grant Award':'Submit Nomination';alert(err.message||err)}}}

async function showReviewQueue(){if(await currentAdminLevel()<2)return;const rows=list(await get(NOMINATIONS_KEY,[])),pending=rows.filter(x=>x.status==='pending'),body=openModal(`<div class="modal-head"><div><div class="eyebrow">ADMIN LEVEL 2</div><h2>Award Review Queue</h2></div><button class="close lkaw-close">×</button></div><div class="lkaw-review-list">${pending.length?pending.map(x=>{const def=byId.get(x.awardId);return `<div class="lkaw-review-row" data-request-id="${esc(x.id)}"><img src="${esc(def?.image||'./assets/lotkeys-default-logo.png')}" alt=""><div><strong>${esc(x.targetUserName)} · ${esc(def?.name||x.awardId)}</strong><small>${esc(x.kind==='user-request'?'User request':'Admin nomination')} from ${esc(x.requestedByUserName||'LotKeys User')}</small><p>${esc(x.reason||'No reason supplied')}</p></div><div><button class="btn primary" data-lkaw-review="approved">Approve</button><button class="btn danger" data-lkaw-review="denied">Deny</button></div></div>`}).join(''):'<div class="empty">No award requests are waiting.</div>'}</div>`);body.querySelectorAll('[data-lkaw-review]').forEach(btn=>btn.onclick=async()=>{const row=btn.closest('[data-request-id]'),request=rows.find(x=>x.id===row.dataset.requestId),status=btn.dataset.lkawReview;if(!request)return;body.querySelectorAll('button').forEach(x=>x.disabled=true);try{if(status==='approved')await grantStoreAward(request.targetUserName,request.awardId,`request:${request.id}`,{source:request.kind==='user-request'?'Approved user request':'Approved Admin Level 1 nomination',approvedBy:await get('userName',''),reason:request.reason},false);request.status=status;request.resolvedAt=new Date().toISOString();request.resolvedByUserName=await get('userName','');await set(NOMINATIONS_KEY,rows);await bridge()?.DriveSync?.syncStoreConfig?.();toast(status==='approved'?'Award approved and granted ✓':'Award request denied');await showReviewQueue();scheduleDecorate()}catch(err){alert(err.message||err);await showReviewQueue()}})}

async function decoratePublicProfile(){const shell=document.querySelector('.public-profile-shell[data-lotkeys-user-name]');if(!shell||shell.dataset.lkawDecorated==='1')return;const users=list(await get('storeUsers',[])),user=users.find(x=>same(x.userName,shell.dataset.lotkeysUserName));if(!user)return;const entries=entriesFrom(user.awards?.length?user.awards:user.unlockedBadges),profileRaw=user.awardProfile||{primaryAwardId:user.primaryAwardId,displayAwardIds:user.displayAwardIds},valid=new Set(entries.map(x=>x.id)),primaryId=valid.has(profileRaw?.primaryAwardId)?profileRaw.primaryAwardId:(defFor(user.favoriteBadge)?.id||entries[0]?.id||''),display=list(profileRaw?.displayAwardIds).filter(x=>valid.has(x)&&x!==primaryId).slice(0,5);for(const e of entries)if(e.id!==primaryId&&!display.includes(e.id)&&display.length<5)display.push(e.id);const primary=entries.find(x=>x.id===primaryId),shown=display.map(id=>entries.find(x=>x.id===id)).filter(Boolean),old=shell.querySelector('details.account-edit-details'),box=document.createElement('div');box.className='lkaw-public-showcase';box.innerHTML=`<small>Primary Award</small>${primary?badgeButton(primary,{large:true,primary:true}):'<div class="help">No Primary Award selected.</div>'}<small>Displayed Awards</small><div class="lkaw-public-five">${shown.length?shown.map(x=>badgeButton(x)).join(''):'<div class="help">No public awards yet.</div>'}</div><button class="btn ghost block" type="button" id="lkaw-public-all" ${entries.length?'':'disabled'}>View All User Awards</button>`;old?.replaceWith(box);if(!old)shell.querySelector('.public-profile-tagline')?.after(box);shell.querySelector('.public-profile-level').textContent=`Lot-Lvl ${Math.max(1,Number(user.lotLevel)||1)} · ${Math.max(0,Number(user.confirmedFacebookPosts)||0)} confirmed posts`;box.querySelector('#lkaw-public-all')?.addEventListener('click',()=>showAllAwards(entries));bindBadgeButtons(box,entries);shell.dataset.lkawDecorated='1'}
function decorateInlinePrimary(root=document){root.querySelectorAll?.('.favorite-badge-inline:not([data-lkaw-inline])').forEach(el=>{const def=defFor(el.textContent.trim());if(!def)return;el.dataset.lkawInline='1';el.removeAttribute('title');el.innerHTML=`<img class="lkaw-inline-award" src="${esc(def.image)}" alt="" aria-hidden="true">`;if(!el.closest('.creator-strip')&&el.parentNode&&el.parentNode.firstChild!==el)el.parentNode.insertBefore(el,el.parentNode.firstChild)})}

let decorating=false,decorateAgain=false;
async function decorate(){if(decorating){decorateAgain=true;return}decorating=true;try{decorateInlinePrimary(document);const title=String(document.getElementById('page-title')?.textContent||'');if(title==='Account')await renderProfileCard();if(title==='Garage'){await renderGarageCard();await renderDevToolkit()}await decoratePublicProfile();await decorateFacebookSaleHistory();await decoratePublicProfileGrant()}catch(err){console.warn('Awards UI decoration deferred',err)}finally{decorating=false;if(decorateAgain){decorateAgain=false;scheduleDecorate()}}}
let decorateTimer=null;function scheduleDecorate(){clearTimeout(decorateTimer);decorateTimer=setTimeout(decorate,40)}
function installStyles(){if(document.getElementById('lotkeys-awards-v09450-styles'))return;const style=document.createElement('style');style.id='lotkeys-awards-v09450-styles';style.textContent=`
.lkaw-profile-card{overflow:hidden}.lkaw-title{display:flex;align-items:flex-end;justify-content:space-between;gap:12px}.lkaw-title h2{margin:2px 0 0;font-size:27px}.lkaw-title>strong{font-size:13px;color:var(--muted)}.lkaw-level-track{height:12px;background:color-mix(in srgb,var(--accent) 10%,var(--line));border-radius:999px;overflow:hidden;margin-top:12px}.lkaw-level-track i{display:block;height:100%;min-width:3px;border-radius:inherit;background:linear-gradient(90deg,var(--accent),color-mix(in srgb,var(--accent) 55%,#facc15))}.lkaw-level-note{font-size:11px;color:var(--muted);margin-top:6px}.lkaw-showcase{display:grid;grid-template-columns:156px minmax(0,1fr);gap:12px;margin-top:18px}.lkaw-primary,.lkaw-five{border:1px solid var(--line);border-radius:17px;padding:10px;background:color-mix(in srgb,var(--accent) 4%,var(--card));min-width:0}.lkaw-primary>small,.lkaw-five>small,.lkaw-public-showcase>small{display:block;color:var(--muted);font-size:10px;font-weight:900;text-transform:uppercase;letter-spacing:.06em;margin-bottom:8px}.lkaw-five>div,.lkaw-public-five{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:7px}.lkaw-badge{border:0;background:transparent;color:var(--ink);padding:2px;min-width:0;text-align:center}.lkaw-badge .lkaw-art{display:block;position:relative;width:100%;aspect-ratio:1;border-radius:15px;background:color-mix(in srgb,var(--accent) 8%,var(--card));overflow:hidden;border:1px solid color-mix(in srgb,var(--accent) 16%,var(--line))}.lkaw-badge img{width:100%;height:100%;object-fit:contain;display:block}.lkaw-badge>span:last-child{display:block;font-size:9px;font-weight:850;line-height:1.2;margin-top:4px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.lkaw-badge .lkaw-art b{position:absolute;right:3px;top:3px;min-width:20px;height:20px;padding:0 4px;border-radius:999px;background:#111827;color:#fff;border:2px solid #fff;display:flex;align-items:center;justify-content:center;font-size:9px}.lkaw-badge.large{width:100%}.lkaw-badge.large .lkaw-art{aspect-ratio:1}.lkaw-badge.large>span:last-child{font-size:11px;white-space:normal}.lkaw-empty-badge{aspect-ratio:1;border:1px dashed var(--line);border-radius:14px;display:flex;flex-direction:column;align-items:center;justify-content:center;color:var(--muted);min-width:0}.lkaw-empty-badge i{font-style:normal;font-size:20px}.lkaw-empty-badge small{font-size:8px}.lkaw-actions .btn{flex:1 1 140px}.lkaw-detail{text-align:center}.lkaw-detail>img{display:block;width:min(300px,82vw);aspect-ratio:1;object-fit:contain;margin:0 auto;border-radius:24px;background:color-mix(in srgb,var(--accent) 6%,var(--card))}.lkaw-detail p{font-size:16px;line-height:1.5;max-width:520px;margin:16px auto 8px}.lkaw-detail>small{display:block;color:var(--muted);margin-top:8px}.lkaw-total{display:inline-flex;border-radius:999px;padding:7px 12px;background:var(--accent);color:var(--accent-ink);font-size:12px;font-weight:900}.lkaw-all-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.lkaw-display-order{border:1px solid color-mix(in srgb,var(--accent) 25%,var(--line));border-radius:16px;padding:10px;margin-top:12px;background:color-mix(in srgb,var(--accent) 5%,var(--card))}.lkaw-display-order>small{display:block;font-size:9px;font-weight:900;color:var(--muted);margin-bottom:7px}.lkaw-display-order>div{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:6px;min-height:70px}.lkaw-reorder-badge{touch-action:none;user-select:none;-webkit-user-select:none;-webkit-touch-callout:none;cursor:grab}.lkaw-reorder-badge.dragging{opacity:.72;cursor:grabbing;transform:scale(1.04)}.lkaw-reorder-cue{position:absolute;right:3px;bottom:3px;width:18px;height:18px;display:grid;place-items:center;border-radius:6px;background:rgba(17,24,39,.82);color:#fff;font-size:11px;font-style:normal}.lkaw-picker-list{margin:12px 0;max-height:56vh;overflow:auto}.lkaw-picker-row{display:grid;grid-template-columns:58px minmax(0,1fr) auto auto;align-items:center;gap:8px;border-bottom:1px solid var(--line);padding:9px 0}.lkaw-picker-row img{width:58px;height:58px;object-fit:contain;border-radius:13px;background:color-mix(in srgb,var(--accent) 6%,var(--card))}.lkaw-picker-row strong,.lkaw-picker-row small{display:block}.lkaw-picker-row small{font-size:10px;color:var(--muted);line-height:1.35;margin-top:2px}.lkaw-picker-row label{font-size:10px;font-weight:850}.lkaw-admin-stats{display:grid;grid-template-columns:1fr 1fr;gap:9px}.lkaw-admin-stats>div{border:1px solid var(--line);border-radius:14px;padding:11px;background:color-mix(in srgb,var(--accent) 5%,var(--card))}.lkaw-admin-stats strong,.lkaw-admin-stats span{display:block}.lkaw-admin-stats strong{font-size:23px}.lkaw-admin-stats span{font-size:10px;color:var(--muted)}.lkaw-review-row{display:grid;grid-template-columns:64px minmax(0,1fr) auto;gap:10px;align-items:center;padding:10px 0;border-bottom:1px solid var(--line)}.lkaw-review-row img{width:64px;height:64px;object-fit:contain;border-radius:14px}.lkaw-review-row strong,.lkaw-review-row small{display:block}.lkaw-review-row small{font-size:10px;color:var(--muted);margin-top:3px}.lkaw-review-row p{font-size:12px;margin:6px 0 0}.lkaw-review-row>div:last-child{display:grid;gap:5px}.lkaw-public-showcase{margin:14px 0;border:1px solid color-mix(in srgb,var(--profile-accent,var(--accent)) 25%,var(--line));border-radius:18px;padding:12px;background:color-mix(in srgb,var(--profile-accent,var(--accent)) 6%,var(--card))}.lkaw-public-showcase>.lkaw-badge.large{max-width:150px;margin:0 auto 13px;display:block}.lkaw-public-five{margin-bottom:10px}.lkaw-inline-award{width:23px;height:23px;object-fit:contain;vertical-align:middle;pointer-events:none}.lkaw-name-tip{position:fixed;z-index:2147483646;background:#111827;color:#fff;border-radius:999px;padding:7px 11px;font-size:12px;font-weight:850;box-shadow:0 8px 24px rgba(0,0,0,.28);pointer-events:none}.lkaw-toast{position:fixed;z-index:2147483647;left:50%;bottom:92px;transform:translateX(-50%);background:#111827;color:#fff;border-radius:12px;padding:11px 15px;font-size:13px}.lkaw-review-list .empty{grid-column:1/-1}
.lkaw-badge{padding:5px}.lkaw-badge .lkaw-art{overflow:visible}.lkaw-badge img{border-radius:inherit}.lkaw-badge .lkaw-art b{right:-5px;top:-5px;z-index:2;min-width:17px;height:17px;padding:0 3px;border-width:1.5px;box-shadow:0 2px 5px rgba(15,23,42,.28);font-size:8px;line-height:1}.lkaw-badge.large .lkaw-art b{right:-4px;top:-4px;min-width:22px;height:22px;padding:0 5px;font-size:10px;border-width:2px}
@media(max-width:560px){.lkaw-showcase{grid-template-columns:116px minmax(0,1fr);gap:8px}.lkaw-primary,.lkaw-five{padding:8px}.lkaw-five>div,.lkaw-public-five{gap:4px}.lkaw-badge>span:last-child{font-size:8px}.lkaw-empty-badge small{display:none}.lkaw-picker-row{grid-template-columns:48px minmax(0,1fr);align-items:start}.lkaw-picker-row img{width:48px;height:48px;grid-row:1/3}.lkaw-picker-row label{grid-column:2}.lkaw-all-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.lkaw-review-row{grid-template-columns:52px minmax(0,1fr)}.lkaw-review-row img{width:52px;height:52px}.lkaw-review-row>div:last-child{grid-column:1/-1;grid-template-columns:1fr 1fr}.lkaw-public-showcase>.lkaw-badge.large{max-width:126px}}
`;document.head.appendChild(style)}

async function init(){if(!bridge())return setTimeout(init,80);installStyles();installCurrentStyles();try{const r=await fetch('./lotkeys-creator-access.json',{cache:'no-store'});if(r.ok)creatorAccess={...creatorAccess,...await r.json()}}catch(err){console.warn('Creator access file unavailable',err)}const saved=list(await get(DEFINITIONS_KEY,[]));if(JSON.stringify(saved.map(x=>x.id))!==JSON.stringify(CATALOG.map(x=>x.id)))await set(DEFINITIONS_KEY,CATALOG.map(x=>({...x})));await importCurrentStoreAwards();if(await currentIsCreator())await grantCurrentAutomatic('lotkeys-developer','creator:permanent','Registered LotKeys Creator');await ensureProgressFromListings();await processCheckIn();const observer=new MutationObserver(scheduleDecorate);observer.observe(document.body,{childList:true,subtree:true});scheduleDecorate();setTimeout(async()=>{if(await currentAdminLevel()>0)await applyAutomaticStoreAwards();await processCelebrationEvents().catch(console.warn)},2200);setInterval(()=>processCelebrationEvents().catch(console.warn),30000)}

async function decorateFacebookSaleHistory(){const shell=document.querySelector('.public-profile-shell[data-lotkeys-user-name]');if(!shell||shell.querySelector('#lkaw-facebook-sale-history')||await currentAdminLevel()<1)return;const users=list(await get('storeUsers',[])),user=users.find(x=>same(x.userName,shell.dataset.lotkeysUserName));if(!user)return;const history=list(user.facebookSaleHistory).slice().sort((a,b)=>Date.parse(b.soldAt||0)-Date.parse(a.soldAt||0)),panel=document.createElement('details');panel.className='account-edit-details';panel.id='lkaw-facebook-sale-history';panel.innerHTML=`<summary><span><strong>Facebook Marketplace Sales</strong><small>${history.length} verified unique sale${history.length===1?'':'s'}</small></span></summary><div class="lkaw-sale-history">${history.length?history.map(x=>`<div><strong>${esc(x.vehicleName||x.vehicleId||'Vehicle Listing')}</strong><small>Posted ${x.postedAt?esc(new Date(x.postedAt).toLocaleDateString('en-CA')):'—'} · Sold ${x.soldAt?esc(new Date(x.soldAt).toLocaleDateString('en-CA')):'—'} · ${esc(x.status||'Sold')}</small><a href="${esc(x.facebookUrl)}" target="_blank" rel="noopener">Open unique Facebook Listing ↗</a></div>`).join(''):'<div class="help">No verified Facebook Marketplace sales recorded yet.</div>'}</div>`;const adminPanel=shell.querySelector('[data-admin-profile-listings]');if(adminPanel)adminPanel.after(panel);else shell.appendChild(panel)}

async function showPublicSpecialGrant(user){if(!await currentIsDeveloper())return;const isCreator=await currentIsCreator(),defs=CATALOG.filter(x=>x.special||(x.creatorOnly&&isCreator)),body=openModal(`<div class="modal-head"><div><div class="eyebrow">DEV-AWARDED ACCOUNT</div><h2>Grant Special Award</h2></div><button class="close lkaw-close">×</button></div><div class="notice"><strong>${esc(user.profileDisplayName||user.userName||'LotKeys User')}</strong><div class="small muted">Permanent account: ${esc(permanentUserKey(user))}</div></div><div class="field"><label>Special Award</label><select id="lkaw-profile-special">${defs.map(x=>`<option value="${esc(x.id)}">${esc(x.name)}</option>`).join('')}</select></div><div class="field"><label>Reason</label><textarea id="lkaw-profile-special-reason" maxlength="500" placeholder="Why is this one-off Award being granted?"></textarea></div><button class="btn primary block" id="lkaw-profile-special-submit" type="button">Grant Special Award</button>`);body.querySelector('#lkaw-profile-special-submit').onclick=async()=>{const reason=body.querySelector('#lkaw-profile-special-reason').value.trim();if(!reason)return toast('Add a reason for the audit record.');const btn=body.querySelector('#lkaw-profile-special-submit');btn.disabled=true;btn.textContent='Granting…';try{const awardId=body.querySelector('#lkaw-profile-special').value,changed=await grantStoreAward(permanentUserKey(user),awardId,uid('PROFILE-DROP'),{source:'Developer grant from public profile',reason});if(changed)await queueDeveloperEvent({title:'A special LotKeys Award!',message:reason,awardId,recipientIds:[permanentUserKey(user)]});await bridge()?.DriveSync?.syncStoreConfig?.();closeModal();toast(changed?'Special Award granted ✓':'That one-off Award is already on this account')}catch(err){btn.disabled=false;btn.textContent='Grant Special Award';alert(err.message||err)}}}

async function decoratePublicProfileGrant(){const shell=document.querySelector('.public-profile-shell[data-lotkeys-user-name]');if(!shell||shell.querySelector('#lkaw-public-special-grant')||!await currentIsDeveloper())return;const users=list(await get('storeUsers',[])),user=users.find(x=>same(x.userName,shell.dataset.lotkeysUserName));if(!user||!permanentUserKey(user))return;const showcase=shell.querySelector('.lkaw-public-showcase');if(!showcase)return;const button=document.createElement('button');button.className='btn primary block';button.id='lkaw-public-special-grant';button.type='button';button.textContent='🎁 Grant Special Award';button.addEventListener('click',()=>showPublicSpecialGrant(user));showcase.appendChild(button)}

function installCurrentStyles(){if(document.getElementById('lotkeys-awards-current-styles'))return;const style=document.createElement('style');style.id='lotkeys-awards-current-styles';style.textContent=`
.lkaw-criteria{display:grid;gap:4px;text-align:left;border:1px solid var(--line);border-radius:14px;padding:11px;margin:12px 0;background:color-mix(in srgb,var(--accent) 4%,var(--card))}.lkaw-criteria strong{font-size:11px;text-transform:uppercase;letter-spacing:.05em}.lkaw-criteria span{font-size:13px;line-height:1.4;color:var(--muted)}
.lkaw-dev-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px}.lkaw-recipient-list{max-height:38vh;overflow:auto;border:1px solid var(--line);border-radius:15px;padding:5px;margin:8px 0 14px}.lkaw-recipient-row{display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:8px;padding:9px;border-bottom:1px solid var(--line)}.lkaw-recipient-row:last-child{border-bottom:0}.lkaw-recipient-row small,.lkaw-all-store small{font-size:10px;color:var(--muted)}.lkaw-all-store{display:flex;align-items:center;gap:8px;font-weight:850;margin-top:8px}.lkaw-sale-history{display:grid;gap:8px;margin-top:10px}.lkaw-sale-history>div{display:grid;gap:3px;border:1px solid var(--line);border-radius:13px;padding:10px}.lkaw-sale-history small{color:var(--muted)}.lkaw-sale-history a{font-size:12px;font-weight:850;color:var(--accent)}.lkaw-event{text-align:center}.lkaw-event img{width:min(280px,70vw);aspect-ratio:1;object-fit:contain;display:block;margin:0 auto 8px}.lkaw-event>strong{font-size:24px}.lkaw-event p{font-size:16px;line-height:1.5}.lkaw-party-icon{font-size:92px;margin:12px}.lkaw-confetti-layer{position:fixed;inset:0;z-index:2147483647;pointer-events:none;overflow:hidden}.lkaw-confetti-layer i{position:absolute;left:50%;top:48%;width:10px;height:18px;border-radius:3px;animation:lkaw-burst var(--d) cubic-bezier(.1,.72,.24,1) forwards}@keyframes lkaw-burst{0%{transform:translate(-50%,-50%) scale(.25);opacity:1}100%{transform:translate(calc(-50% + var(--x)),calc(-50% - var(--y))) rotate(var(--r)) scale(1);opacity:0}}
@media(max-width:560px){.lkaw-dev-grid{grid-template-columns:1fr}.lkaw-recipient-row{grid-template-columns:auto minmax(0,1fr)}.lkaw-recipient-row small{grid-column:2}}
@media(prefers-reduced-motion:reduce){.lkaw-confetti-layer i{animation:none;display:none}}
`;document.head.appendChild(style)}

function accountIdentityKeys(user={}){return new Set([user.googleSub,user.accountSub,user.sub,user.accountId,user.email,user.userName].map(x=>String(x||'').trim().toLowerCase()).filter(Boolean))}
function celebrationRecipientRows(users,currentKeys){return users.filter(x=>x.status!=='disabled'&&permanentUserKey(x)).map(x=>{const key=permanentUserKey(x),self=[...accountIdentityKeys(x)].some(id=>currentKeys.has(id));return `<label class="lkaw-recipient-row"><input type="checkbox" data-lkaw-recipient data-lkaw-self="${self?'1':'0'}" value="${esc(key)}"><span>${esc(x.profileDisplayName||x.userName||x.email||'LotKeys User')}</span>${self?'<small>You · select manually</small>':''}</label>`}).join('')}

function launchCelebrationBurst(){document.querySelector('.lkaw-confetti-layer')?.remove();const layer=document.createElement('div');layer.className='lkaw-confetti-layer';const colors=['#00aeef','#facc15','#fb2c94','#22c55e','#8b5cf6','#fff'];for(let i=0;i<84;i++){const bit=document.createElement('i');bit.style.setProperty('--x',`${(Math.random()-.5)*100}vw`);bit.style.setProperty('--y',`${15+Math.random()*78}vh`);bit.style.setProperty('--r',`${Math.random()*900-450}deg`);bit.style.setProperty('--d',`${.55+Math.random()*.8}s`);bit.style.background=colors[i%colors.length];layer.appendChild(bit)}document.body.appendChild(layer);setTimeout(()=>layer.remove(),2200)}

async function queueDeveloperEvent({title,message,awardId='',recipientIds=[]}={}){const me=await currentUser(),events=list(await get(CELEBRATION_EVENTS_KEY,[])),event={id:uid('DEV-EVENT'),title:String(title||'A LotKeys Celebration!').slice(0,80),message:String(message||'The LotKeys team is celebrating with you.').slice(0,700),awardId:String(awardId||''),recipientIds:[...new Set(recipientIds.map(String).filter(Boolean))],createdAt:new Date().toISOString(),createdByAccountId:permanentUserKey(me||{}),createdByUserName:String(me?.userName||await get('userName',''))};events.push(event);await set(CELEBRATION_EVENTS_KEY,events.slice(-500));return event}

async function showDeveloperComposer(mode='celebration',draft={}){
  if(!await currentIsDeveloper())return;
  const users=list(await get('storeUsers',[])),me=await currentUser(),isCreator=await currentIsCreator();
  const currentKeys=new Set([...accountIdentityKeys(me||{}),String(await get('currentAccountSub','')).trim().toLowerCase(),String(await get('currentAccountEmail','')).trim().toLowerCase(),String(await get('userName','')).trim().toLowerCase()].filter(Boolean));
  const awardChoices=CATALOG.filter(x=>x.special||(x.creatorOnly&&isCreator)).map(x=>`<option value="${esc(x.id)}">${esc(x.name)}</option>`).join('');
  const requiresAward=mode==='drop',defaultTitle=mode==='celebration'?'Store-wide Celebration!':'A special LotKeys Award';
  const body=openModal(`<div class="modal-head"><div><div class="eyebrow">DEV TOOL KIT</div><h2>${mode==='celebration'?'Store-wide Celebration':'Special Award Drop'}</h2></div><button class="close lkaw-close">×</button></div><div class="field"><label>Headline</label><input id="lkaw-dev-title" maxlength="80" value="${esc(draft.title??defaultTitle)}"></div><div class="field"><label>Message</label><textarea id="lkaw-dev-message" maxlength="700" placeholder="Write the bubble-window message…">${esc(draft.message||'')}</textarea></div><div class="field"><label>${requiresAward?'Special Award':'Award · optional'}</label><select id="lkaw-dev-award">${requiresAward?'':'<option value="">Celebration only</option>'}${awardChoices}</select></div><label class="lkaw-all-store"><input id="lkaw-all-store" type="checkbox"> All users in Store <small>(your own account stays unchecked)</small></label><div class="lkaw-recipient-list">${celebrationRecipientRows(users,currentKeys)||'<div class="empty">No Store users are available.</div>'}</div><div class="actions"><button class="btn ghost" id="lkaw-dev-preview" type="button">Preview Celebration</button><button class="btn primary" id="lkaw-dev-send" type="button">${mode==='celebration'?'Launch Celebration':'Drop Special Award'}</button></div><div class="help">Recipients are tied to permanent LotKeys account IDs. Special badges are one-off and never increase sales or milestone totals.</div>`);
  if(!body)return;
  const all=body.querySelector('#lkaw-all-store'),checks=[...body.querySelectorAll('[data-lkaw-recipient]')];
  const selectedDraft=new Set(list(draft.recipientIds).map(String));
  checks.forEach(x=>{x.checked=selectedDraft.has(x.value)});all.checked=!!draft.allStore;
  const awardSelect=body.querySelector('#lkaw-dev-award');if(draft.awardId&&[...awardSelect.options].some(x=>x.value===String(draft.awardId)))awardSelect.value=String(draft.awardId);
  all.onchange=()=>checks.forEach(x=>{x.checked=all.checked&&x.dataset.lkawSelf!=='1'});
  const readDraft=()=>({title:body.querySelector('#lkaw-dev-title').value.trim(),message:body.querySelector('#lkaw-dev-message').value.trim(),awardId:String(body.querySelector('#lkaw-dev-award').value||''),recipientIds:checks.filter(x=>x.checked).map(x=>x.value),allStore:all.checked});
  body.querySelector('#lkaw-dev-preview').onclick=()=>{
    const preview=readDraft(),def=byId.get(preview.awardId),previewBody=openModal(`<div class="modal-head"><div><div class="eyebrow">PREVIEW · NOTHING WILL BE SENT</div><h2>${esc(preview.title||defaultTitle)}</h2></div><button class="close lkaw-close">×</button></div><div class="lkaw-event">${def?`<img src="${esc(def.image)}" alt="${esc(def.name)}"><strong>${esc(def.name)}</strong>`:'<div class="lkaw-party-icon">🎉</div>'}<p>${esc(preview.message||'The LotKeys team is celebrating with you.')}</p><button class="btn primary block" id="lkaw-preview-back" type="button">Back to Editor</button></div>`);
    launchCelebrationBurst();previewBody?.querySelector('#lkaw-preview-back')?.addEventListener('click',()=>showDeveloperComposer(mode,preview));previewBody?.querySelector('.lkaw-close')?.addEventListener('click',()=>showDeveloperComposer(mode,preview),{once:true});
  };
  body.querySelector('#lkaw-dev-send').onclick=async()=>{
    const current=readDraft(),selected=current.recipientIds,awardId=current.awardId,title=current.title,message=current.message;
    if(!selected.length)return toast('Choose at least one recipient.');
    if(requiresAward&&!awardId)return toast('Choose a special Award.');
    const btn=body.querySelector('#lkaw-dev-send');btn.disabled=true;btn.textContent='Sending…';
    try{
      if(awardId)for(const id of selected)await grantStoreAward(id,awardId,uid('DEV-DROP'),{source:'Developer special Award drop',reason:message||title},false);
      await queueDeveloperEvent({title,message,awardId,recipientIds:selected});
      await bridge()?.DriveSync?.syncStoreConfig?.();
      closeModal();launchCelebrationBurst();toast(`${selected.length} LotKeys account${selected.length===1?'':'s'} selected ✓`);
    }catch(err){btn.disabled=false;btn.textContent=mode==='celebration'?'Launch Celebration':'Drop Special Award';alert(err.message||err)}
  };
}

async function renderDevToolkit(){const main=document.getElementById('main');if(!main||document.getElementById('lkaw-dev-toolkit')||String(document.getElementById('page-title')?.textContent)!=='Garage'||!await currentIsDeveloper())return;const card=document.createElement('div');card.className='card lkaw-dev-toolkit';card.id='lkaw-dev-toolkit';card.innerHTML=`<div class="section-title" style="margin-top:0"><div><h2>🧰 Dev Tool Kit</h2><small>Creator-authorized fun tools</small></div></div><div class="lkaw-dev-grid"><button class="btn primary" data-lkaw-dev="celebration">🎉 Store-wide Celebration</button><button class="btn" data-lkaw-dev="drop">🎁 Special Award Drop</button></div><div class="help">Build and preview the message inside either tool. All Store sends exclude your own account until you select it manually. Every special grant records issuer, recipient, time and reason.</div>`;main.appendChild(card);card.querySelector('[data-lkaw-dev="celebration"]').onclick=()=>showDeveloperComposer('celebration');card.querySelector('[data-lkaw-dev="drop"]').onclick=()=>showDeveloperComposer('drop')}

async function processCelebrationEvents(){
  const me=await currentUser(),keys=new Set([permanentUserKey(me||{}),String(me?.userName||'').toLowerCase(),String(me?.email||'').toLowerCase(),String(await get('currentAccountSub','')).toLowerCase(),String(await get('currentAccountEmail','')).toLowerCase(),String(await get('userName','')).toLowerCase()].filter(Boolean)),seen=new Set(list(await get(SEEN_CELEBRATIONS_KEY,[])).map(String));
  const queue=list(await get(CELEBRATION_EVENTS_KEY,[])).filter(x=>x?.id&&!seen.has(String(x.id))&&list(x.recipientIds).some(id=>keys.has(String(id).toLowerCase())));
  if(!queue.length)return;
  launchCelebrationBurst();
  for(let index=0;index<queue.length;index++){
    const event=queue[index],def=byId.get(event.awardId),body=openModal(`<div class="modal-head"><div><div class="eyebrow">A LOTKEYS DROP</div><h2>${esc(event.title||'A LotKeys Celebration!')}</h2></div><button class="close lkaw-close">×</button></div><div class="lkaw-event">${def?`<img src="${esc(def.image)}" alt="${esc(def.name)}"><strong>${esc(def.name)}</strong>`:'<div class="lkaw-party-icon">🎉</div>'}<p>${esc(event.message||'The LotKeys team is celebrating with you.')}</p><button class="btn primary block" id="lkaw-event-next" type="button">${index===queue.length-1?'Awesome!':'Next'}</button></div>`);
    await new Promise(resolve=>{let finished=false;const done=()=>{if(finished)return;finished=true;if(document.getElementById('modal')?.open)closeModal();resolve()};body?.querySelector('#lkaw-event-next')?.addEventListener('click',done);body?.querySelector('.lkaw-close')?.addEventListener('click',done,{once:true})});
    seen.add(String(event.id));
  }
  await set(SEEN_CELEBRATIONS_KEY,[...seen].slice(-1000));queueAccountSync();
}

window.LotKeysAwards={version:VERSION,catalog:CATALOG,levelThreshold,levelForPosts,canonicalFacebookMarketplaceUrl,validFacebookUrl,validateVehicleSale,confirmFacebookPost,recordVehicleProfileCreated,recordListingCreated,recordListingDeleted,recordVehicleSale,recordApprovedProfileCorrection,applyAutomaticStoreAwards,reconcileCurrentAccount:reconcileAccountAccess,isCreator:currentIsCreator,isDeveloper:currentIsDeveloper,checkDeveloperEvents:processCelebrationEvents,refresh:scheduleDecorate};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
